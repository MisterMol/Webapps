* {
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    margin: 0;
    font-family: var(--font-main);
    background:
        radial-gradient(circle at top left, color-mix(in srgb, var(--color-accent) 16%, transparent), transparent 34rem),
        linear-gradient(180deg, #ffffff 0%, var(--color-background) 42%);
    color: var(--color-text);
    font-size: 17px;
    line-height: 1.6;
}

a {
    color: inherit;
    text-decoration: none;
}

img {
    max-width: 100%;
}

.container {
    width: min(var(--page-width), calc(100% - 32px));
    margin: 0 auto;
}

.site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    background: color-mix(in srgb, var(--color-primary) 94%, #ffffff);
    color: white;
    box-shadow: 0 10px 30px rgba(15, 23, 42, 0.16);
}

.header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    padding: 16px 0;
}

.brand {
    display: flex;
    align-items: center;
    gap: 12px;
    font-weight: 800;
    font-size: 20px;
    letter-spacing: -0.02em;
}

.brand-logo,
.brand-mark {
    width: 65px;
    height: 65px;
    border-radius: 14px;
    background: white;
    color: var(--color-primary);
    display: grid;
    place-items: center;
    font-weight: 900;
    object-fit: contain;
    padding: 5px;
}

.brand-text {
    white-space: nowrap;
}

.nav {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 8px;
}

.nav a {
    padding: 10px 13px;
    border-radius: 999px;
    opacity: 0.94;
    transition: background 160ms ease, transform 160ms ease, opacity 160ms ease;
}

.nav a:hover {
    background: rgba(255, 255, 255, 0.13);
    transform: translateY(-1px);
    opacity: 1;
}

.page {
    padding: 34px 0 56px;
}

.hero {
    background:
        linear-gradient(135deg, color-mix(in srgb, var(--color-primary) 92%, #ffffff), color-mix(in srgb, var(--color-primary) 72%, var(--color-accent))),
        var(--color-primary);
    color: white;
    border-radius: calc(var(--radius-main) + 10px);
    padding: clamp(28px, 5vw, 54px);
    box-shadow: 0 24px 70px rgba(15, 23, 42, 0.22);
    margin-bottom: 30px;
    overflow: hidden;
    position: relative;
}

.hero::after {
    content: "";
    position: absolute;
    inset: auto -70px -120px auto;
    width: 280px;
    height: 280px;
    background: color-mix(in srgb, var(--color-accent) 40%, transparent);
    border-radius: 50%;
    filter: blur(6px);
}

.hero > * {
    position: relative;
    z-index: 1;
}

.hero h1 {
    margin: 0 0 12px;
    font-size: clamp(34px, 6vw, 62px);
    line-height: 1.05;
    letter-spacing: -0.05em;
}

.hero p {
    max-width: 720px;
    margin: 0 0 22px;
    font-size: clamp(18px, 2vw, 22px);
    opacity: 0.92;
}

.button,
button.button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    background: var(--color-accent);
    color: #111827;
    padding: 12px 18px;
    border-radius: 999px;
    font-weight: 800;
    border: 0;
    cursor: pointer;
    box-shadow: 0 10px 22px color-mix(in srgb, var(--color-accent) 26%, transparent);
    transition: transform 160ms ease, box-shadow 160ms ease, filter 160ms ease;
}

.button:hover,
button.button:hover {
    transform: translateY(-2px);
    filter: brightness(1.03);
    box-shadow: 0 14px 28px color-mix(in srgb, var(--color-accent) 34%, transparent);
}

.button.secondary {
    background: var(--color-card);
    color: var(--color-text);
    box-shadow: inset 0 0 0 1px var(--color-border);
}

.section-title {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 18px;
    margin: 32px 0 18px;
}

.section-title h2 {
    margin: 0;
    font-size: clamp(24px, 3vw, 34px);
    letter-spacing: -0.03em;
}

.section-title p {
    margin: 0;
    color: var(--color-muted);
}

.filter-bar {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin: 20px 0 24px;
}

.filter-chip {
    display: inline-flex;
    align-items: center;
    min-height: 42px;
    border-radius: 999px;
    padding: 9px 14px;
    background: var(--color-card);
    color: var(--color-text);
    border: 1px solid var(--color-border);
    transition: transform 160ms ease, border 160ms ease, background 160ms ease;
}

.filter-chip:hover,
.filter-chip.active {
    transform: translateY(-1px);
    border-color: color-mix(in srgb, var(--color-accent) 60%, var(--color-border));
    background: color-mix(in srgb, var(--color-accent) 12%, var(--color-card));
}

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
    gap: 22px;
}

.card {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    overflow: hidden;
    box-shadow: 0 12px 34px rgba(15, 23, 42, 0.08);
    transition: transform 180ms ease, box-shadow 180ms ease, border 180ms ease;
}

a.card:hover,
.card.interactive:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 50px rgba(15, 23, 42, 0.14);
    border-color: color-mix(in srgb, var(--color-accent) 50%, var(--color-border));
}

.card-image-wrap {
    position: relative;
    aspect-ratio: 4 / 3;
    background:
        linear-gradient(135deg, color-mix(in srgb, var(--color-primary) 8%, #ffffff), color-mix(in srgb, var(--color-accent) 16%, #ffffff));
    overflow: hidden;
}

.card-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 250ms ease;
}

a.card:hover .card-image {
    transform: scale(1.035);
}

.placeholder-image {
    height: 100%;
    display: grid;
    place-items: center;
    padding: 26px;
    text-align: center;
    color: var(--color-muted);
    font-weight: 800;
}

.card-body {
    padding: 18px;
}

.card-body h2,
.card-body h3 {
    margin: 0 0 8px;
    line-height: 1.18;
    letter-spacing: -0.025em;
}

.card-body p {
    margin: 0 0 10px;
}

.meta {
    color: var(--color-muted);
    font-size: 14px;
    font-weight: 700;
}

.price {
    font-weight: 900;
    font-size: 19px;
    margin-top: 12px;
    color: var(--color-primary);
}

.badges {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin-top: 12px;
}

.badge {
    display: inline-flex;
    border-radius: 999px;
    padding: 5px 9px;
    background: color-mix(in srgb, var(--color-accent) 14%, var(--color-card));
    color: var(--color-text);
    font-size: 13px;
    font-weight: 800;
}

.detail-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(280px, 0.95fr);
    gap: 24px;
    align-items: start;
}

.detail-media {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    overflow: hidden;
    box-shadow: 0 16px 44px rgba(15, 23, 42, 0.10);
}

.detail-image {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    display: block;
}

.table-wrap {
    overflow-x: auto;
    border-radius: var(--radius-main);
    box-shadow: 0 12px 34px rgba(15, 23, 42, 0.08);
    border: 1px solid var(--color-border);
}

.table {
    width: 100%;
    border-collapse: collapse;
    background: var(--color-card);
}

.table th,
.table td {
    padding: 14px 15px;
    border-bottom: 1px solid var(--color-border);
    text-align: left;
    vertical-align: top;
}

.table th {
    font-size: 14px;
    color: var(--color-muted);
    background: color-mix(in srgb, var(--color-background) 72%, #ffffff);
}

.table tr:last-child td {
    border-bottom: 0;
}

.low-stock {
    color: #b91c1c;
    font-weight: 900;
}

.form-panel {
    max-width: 720px;
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    padding: 26px;
    box-shadow: 0 16px 44px rgba(15, 23, 42, 0.10);
}

.form-row {
    margin-bottom: 16px;
}

.form-row label {
    display: block;
    font-weight: 800;
    margin-bottom: 6px;
}

.form-row input,
.form-row textarea,
.form-row select {
    width: 100%;
    min-height: 46px;
    padding: 11px 12px;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    background: #ffffff;
    color: var(--color-text);
    font: inherit;
}

.form-row textarea {
    min-height: 110px;
}

.messages {
    padding-top: 16px;
}

.message {
    background: #ecfdf5;
    border: 1px solid #10b981;
    border-radius: 14px;
    padding: 12px 14px;
}

.empty-state {
    background: var(--color-card);
    border: 1px dashed var(--color-border);
    border-radius: var(--radius-main);
    padding: 30px;
    text-align: center;
    color: var(--color-muted);
}

@media (max-width: 760px) {
    body {
        font-size: 16px;
    }

    .header-inner {
        align-items: flex-start;
        flex-direction: column;
    }

    .nav {
        justify-content: flex-start;
    }

    .nav a {
        background: rgba(255, 255, 255, 0.10);
    }

    .detail-layout {
        grid-template-columns: 1fr;
    }

    .hero {
        padding: 28px;
    }
}

.nav-form {
    margin: 0;
}

.nav-form button {
    font: inherit;
    color: white;
    background: rgba(255, 255, 255, 0.12);
    border: 0;
    padding: 10px 13px;
    border-radius: 999px;
    cursor: pointer;
    transition: background 160ms ease, transform 160ms ease;
}

.nav-form button:hover {
    background: rgba(255, 255, 255, 0.20);
    transform: translateY(-1px);
}

.help-block {
    color: var(--color-muted);
    font-size: 14px;
    margin-top: 6px;
}

.form-panel select[multiple] {
    min-height: 130px;
}

.badge-positive {
    background: #dcfce7;
    color: #166534;
}

.badge-negative {
    background: #fee2e2;
    color: #991b1b;
}

.badge-warning {
    background: #fef3c7;
    color: #92400e;
}

.movement-row.positive td {
    background: color-mix(in srgb, #dcfce7 42%, var(--color-card));
}

.movement-row.negative td {
    background: color-mix(in srgb, #fee2e2 42%, var(--color-card));
}

.movement-row.positive td:nth-child(4) {
    color: #166534;
}

.movement-row.negative td:nth-child(4) {
    color: #991b1b;
}

.stock-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(320px, 420px);
    gap: 24px;
    align-items: start;
}

.stock-side-card {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    padding: 24px;
    box-shadow: 0 16px 44px rgba(15, 23, 42, 0.10);
    position: sticky;
    top: 96px;
}

.stock-current {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    border: 1px solid var(--color-border);
    border-radius: 14px;
    padding: 13px 14px;
    margin-bottom: 10px;
}

.stock-current strong {
    font-size: 22px;
}

.mini-mutations {
    display: grid;
    gap: 10px;
}

.mini-mutation {
    border-radius: 14px;
    padding: 12px;
    border: 1px solid var(--color-border);
    display: grid;
    gap: 3px;
}

.mini-mutation.positive {
    background: #dcfce7;
    color: #166534;
}

.mini-mutation.negative {
    background: #fee2e2;
    color: #991b1b;
}

.mini-mutation span,
.mini-mutation small {
    display: block;
}

.mini-mutation small {
    opacity: 0.78;
}

input[disabled] {
    background: #f1f5f9;
    color: #64748b;
    cursor: not-allowed;
}

@media (max-width: 920px) {
    .stock-layout {
        grid-template-columns: 1fr;
    }

    .stock-side-card {
        position: static;
    }
}

.compact-hero {
    padding: clamp(24px, 4vw, 42px);
}

.quick-actions {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
    gap: 18px;
    margin-bottom: 34px;
}

.action-card {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    padding: 22px;
    box-shadow: 0 12px 34px rgba(15, 23, 42, 0.08);
    transition: transform 180ms ease, box-shadow 180ms ease, border 180ms ease;
}

.action-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 50px rgba(15, 23, 42, 0.14);
    border-color: color-mix(in srgb, var(--color-accent) 55%, var(--color-border));
}

.action-card span {
    display: inline-flex;
    background: color-mix(in srgb, var(--color-accent) 16%, var(--color-card));
    color: var(--color-primary);
    border-radius: 999px;
    padding: 5px 10px;
    font-size: 13px;
    font-weight: 900;
    margin-bottom: 12px;
}

.action-card strong {
    display: block;
    font-size: 21px;
    letter-spacing: -0.03em;
    margin-bottom: 8px;
}

.action-card small {
    color: var(--color-muted);
    font-size: 15px;
}

.smart-form-shell {
    display: grid;
    gap: 20px;
}

.smart-form {
    display: grid;
    gap: 22px;
}

.form-section {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    padding: 24px;
    box-shadow: 0 16px 44px rgba(15, 23, 42, 0.08);
    animation: sectionIn 220ms ease both;
}

@keyframes sectionIn {
    from {
        opacity: 0;
        transform: translateY(8px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.form-section-header {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    margin-bottom: 18px;
}

.form-section-header h2 {
    margin: 0;
    letter-spacing: -0.03em;
}

.form-section-header p {
    margin: 2px 0 0;
    color: var(--color-muted);
}

.step-dot {
    width: 34px;
    height: 34px;
    border-radius: 999px;
    background: var(--color-primary);
    color: white;
    display: grid;
    place-items: center;
    font-weight: 900;
    flex: 0 0 auto;
}

.form-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 16px;
}

.field-help {
    color: var(--color-muted);
    font-size: 14px;
    font-style: italic;
    margin: 6px 0 0;
}

.help-icon {
    display: inline-grid;
    place-items: center;
    width: 19px;
    height: 19px;
    margin-left: 6px;
    border-radius: 999px;
    background: #e2e8f0;
    color: #334155;
    font-size: 12px;
    font-weight: 900;
    cursor: help;
}

.help-icon:hover {
    background: var(--color-accent);
    color: #111827;
}

.toggle-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 14px;
    border: 1px solid var(--color-border);
    border-radius: 14px;
    background: color-mix(in srgb, var(--color-background) 70%, #ffffff);
    margin-bottom: 12px;
}

.toggle-row input {
    width: 22px;
    height: 22px;
    margin-top: 2px;
    accent-color: var(--color-accent);
}

.toggle-row label {
    display: grid;
    gap: 2px;
    cursor: pointer;
}

.toggle-row label span {
    color: var(--color-muted);
    font-size: 14px;
    font-style: italic;
}

.stock-start-panel {
    border-left: 4px solid var(--color-accent);
    background: color-mix(in srgb, var(--color-accent) 8%, var(--color-card));
    border-radius: 14px;
    padding: 18px;
    margin-top: 14px;
}

.stock-start-panel h3 {
    margin-top: 0;
}

.label-picker {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

.label-chip-option {
    cursor: pointer;
}

.label-chip-option input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
}

.label-chip-option span {
    display: inline-flex;
    border: 1px solid var(--color-border);
    background: var(--color-card);
    border-radius: 999px;
    padding: 9px 13px;
    font-weight: 800;
    transition: background 160ms ease, transform 160ms ease, border 160ms ease;
}

.label-chip-option input:checked + span {
    background: color-mix(in srgb, var(--color-accent) 25%, var(--color-card));
    border-color: var(--color-accent);
    transform: translateY(-1px);
}

.sticky-actions {
    position: sticky;
    bottom: 14px;
    z-index: 10;
    background: color-mix(in srgb, var(--color-card) 88%, transparent);
    backdrop-filter: blur(12px);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    padding: 14px;
    box-shadow: 0 16px 44px rgba(15, 23, 42, 0.16);
}

.media-strip {
    display: flex;
    gap: 10px;
    padding: 12px;
    overflow-x: auto;
    background: var(--color-card);
    border-top: 1px solid var(--color-border);
}

.media-thumb {
    width: 76px;
    height: 58px;
    border-radius: 12px;
    border: 1px solid var(--color-border);
    overflow: hidden;
    display: grid;
    place-items: center;
    flex: 0 0 auto;
    background: #f8fafc;
    color: var(--color-muted);
    font-size: 12px;
    font-weight: 800;
}

.media-thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.form-row input[type="file"] {
    padding: 14px;
    background: color-mix(in srgb, var(--color-background) 70%, #ffffff);
    border-style: dashed;
}

@media (max-width: 760px) {
    .form-section {
        padding: 18px;
    }

    .sticky-actions {
        position: static;
    }
}

.row-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.button.danger,
button.danger {
    background: #fee2e2;
    color: #991b1b;
    box-shadow: none;
}

.button.danger:hover,
button.danger:hover {
    background: #fecaca;
}

.browse-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin: 0 0 22px;
}

.browse-tabs a {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    min-height: 44px;
    padding: 10px 15px;
    border-radius: 999px;
    background: var(--color-card);
    border: 1px solid var(--color-border);
    font-weight: 900;
    transition: transform 160ms ease, border 160ms ease, background 160ms ease;
}

.browse-tabs a:hover,
.browse-tabs a.active {
    transform: translateY(-1px);
    background: color-mix(in srgb, var(--color-accent) 16%, var(--color-card));
    border-color: color-mix(in srgb, var(--color-accent) 65%, var(--color-border));
}

.browse-tabs span {
    display: inline-grid;
    place-items: center;
    min-width: 26px;
    min-height: 26px;
    border-radius: 999px;
    background: color-mix(in srgb, var(--color-primary) 10%, var(--color-card));
    color: var(--color-primary);
    font-size: 13px;
}

.filter-panel {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    padding: 18px;
    box-shadow: 0 12px 34px rgba(15, 23, 42, 0.08);
    margin-bottom: 26px;
}

.filter-grid {
    display: grid;
    grid-template-columns: minmax(260px, 2fr) repeat(auto-fit, minmax(190px, 1fr));
    gap: 14px;
}

.item-browser-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(310px, 1fr));
    gap: 18px;
}

.browser-card {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-main);
    padding: 18px;
    box-shadow: 0 12px 34px rgba(15, 23, 42, 0.08);
    transition: transform 180ms ease, box-shadow 180ms ease, border 180ms ease;
}

.browser-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 20px 50px rgba(15, 23, 42, 0.14);
    border-color: color-mix(in srgb, var(--color-accent) 45%, var(--color-border));
}

.browser-card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 14px;
    margin-bottom: 10px;
}

.browser-card-header h2 {
    margin: 0;
    letter-spacing: -0.03em;
}

.browser-meta {
    display: grid;
    gap: 5px;
    color: var(--color-muted);
    font-size: 14px;
    margin: 12px 0;
}

.recipe-mini-status {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 12px 0;
}

.fact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 12px;
    margin: 18px 0;
}

.fact-grid > div {
    border: 1px solid var(--color-border);
    border-radius: 14px;
    padding: 13px;
    background: color-mix(in srgb, var(--color-background) 70%, #ffffff);
}

.fact-grid span,
.fact-grid strong {
    display: block;
}

.fact-grid strong {
    font-size: 18px;
    margin-top: 3px;
}

.table-link {
    font-weight: 900;
    color: var(--color-text);
    text-decoration: underline;
    text-decoration-thickness: 2px;
    text-decoration-color: color-mix(in srgb, var(--color-accent) 65%, transparent);
    text-underline-offset: 3px;
}

.table-link:hover {
    color: color-mix(in srgb, var(--color-primary) 70%, var(--color-accent));
}

.edit-layout {
    display: grid;
    grid-template-columns: minmax(260px, 340px) minmax(0, 1fr);
    gap: 22px;
    align-items: start;
}

.edit-side-panel {
    display: grid;
    gap: 16px;
    position: sticky;
    top: 18px;
}

.hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 18px;
}

.media-preview-grid {
    display: grid;
    gap: 10px;
}

.media-preview {
    border: 1px solid var(--color-border);
    border-radius: 14px;
    overflow: hidden;
    background: #f8fafc;
}

.media-preview img,
.media-preview video {
    display: block;
    width: 100%;
    max-height: 180px;
    object-fit: cover;
}

.media-preview .badge {
    margin: 10px;
}

.mini-row {
    display: grid;
    gap: 2px;
    padding: 10px 0;
    border-bottom: 1px solid var(--color-border);
}

.mini-row:last-child {
    border-bottom: 0;
}

.mini-row span {
    color: var(--color-muted);
    font-size: 14px;
}

@media (max-width: 980px) {
    .edit-layout {
        grid-template-columns: 1fr;
    }

    .edit-side-panel {
        position: static;
    }
}


/* GraafMol dark public theme */

body {
    background:
        radial-gradient(circle at top left, rgba(200, 155, 60, 0.18), transparent 34rem),
        radial-gradient(circle at top right, rgba(255, 255, 255, 0.07), transparent 30rem),
        linear-gradient(180deg, #090909 0%, #11100f 44%, #171410 100%);
    color: var(--color-text);
}

.site-header {
    background: rgba(8, 8, 8, 0.88);
    backdrop-filter: blur(18px);
    border-bottom: 1px solid rgba(200, 155, 60, 0.22);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.36);
}

.brand-logo,
.brand-mark {
    background: linear-gradient(135deg, #1c1710, #090909);
    color: var(--color-accent);
    border: 1px solid rgba(200, 155, 60, 0.38);
    box-shadow: inset 0 0 0 1px rgba(255,255,255,0.04), 0 10px 26px rgba(0,0,0,0.28);
}

.brand-text {
    color: #f6efe3;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    font-size: 15px;
}

.nav a,
.nav-form button {
    color: #f6efe3;
    background: transparent;
    border: 1px solid transparent;
}

.nav a:hover,
.nav a.active,
.nav-form button:hover {
    background: rgba(200, 155, 60, 0.12);
    border-color: rgba(200, 155, 60, 0.26);
}

.nav-login {
    border-color: rgba(200, 155, 60, 0.42) !important;
    color: var(--color-accent) !important;
}

.page {
    padding-top: 38px;
}

.public-hero {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr);
    gap: clamp(24px, 4vw, 54px);
    align-items: center;
    min-height: 620px;
    padding: clamp(28px, 5vw, 64px);
    border: 1px solid rgba(200, 155, 60, 0.24);
    border-radius: calc(var(--radius-main) + 14px);
    background:
        linear-gradient(135deg, rgba(255,255,255,0.05), transparent 32%),
        radial-gradient(circle at 74% 26%, rgba(200, 155, 60, 0.20), transparent 22rem),
        linear-gradient(135deg, #15110c 0%, #090909 62%, #1b1711 100%);
    box-shadow: 0 28px 90px rgba(0,0,0,0.46);
    overflow: hidden;
    position: relative;
}

.public-hero::before {
    content: "";
    position: absolute;
    inset: 18px;
    border: 1px solid rgba(200, 155, 60, 0.16);
    border-radius: calc(var(--radius-main) + 4px);
    pointer-events: none;
}

.public-hero-content,
.public-hero-media {
    position: relative;
    z-index: 1;
}

.eyebrow {
    color: var(--color-accent);
    font-weight: 900;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    font-size: 13px;
    margin: 0 0 18px;
}

.public-hero h1 {
    margin: 0 0 20px;
    max-width: 780px;
    font-size: clamp(42px, 7vw, 82px);
    line-height: 0.96;
    letter-spacing: -0.065em;
    color: #fff8ec;
}

.public-hero p {
    color: #cfc6b7;
    max-width: 680px;
    font-size: clamp(18px, 2vw, 22px);
    margin: 0 0 24px;
}

.public-hero-media {
    border-radius: calc(var(--radius-main) + 8px);
    overflow: hidden;
    background: #0d0d0d;
    border: 1px solid rgba(200, 155, 60, 0.25);
    box-shadow: 0 24px 70px rgba(0,0,0,0.42);
}

.public-hero-media img {
    width: 100%;
    height: 100%;
    min-height: 430px;
    object-fit: contain;
    display: block;
    padding: 16px;
    background:
        radial-gradient(circle at center, rgba(200,155,60,0.08), transparent 55%),
        #090909;
}

.hero-placeholder {
    min-height: 430px;
    display: grid;
    place-items: center;
    text-align: center;
    color: var(--color-accent);
}

.hero-placeholder span {
    display: block;
    font-size: 96px;
    font-weight: 900;
    line-height: 1;
}

.hero-placeholder small {
    display: block;
    margin-top: 12px;
    color: var(--color-muted);
    letter-spacing: 0.18em;
    text-transform: uppercase;
}

.public-usps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 18px;
    margin: 28px 0 44px;
}

.public-usps article {
    background:
        linear-gradient(135deg, rgba(255,255,255,0.04), transparent),
        var(--color-card);
    border: 1px solid rgba(200, 155, 60, 0.18);
    border-radius: var(--radius-main);
    padding: 22px;
    box-shadow: 0 18px 46px rgba(0,0,0,0.22);
    transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
}

.public-usps article:hover {
    transform: translateY(-4px);
    border-color: rgba(200, 155, 60, 0.38);
    box-shadow: 0 24px 60px rgba(0,0,0,0.32);
}

.public-usps span {
    color: var(--color-accent);
    font-weight: 900;
    letter-spacing: 0.18em;
    font-size: 13px;
}

.public-usps h2 {
    color: #fff8ec;
    margin: 10px 0 8px;
    letter-spacing: -0.03em;
}

.public-usps p {
    color: var(--color-muted);
    margin: 0;
}

.hero {
    background:
        radial-gradient(circle at top right, rgba(200,155,60,0.22), transparent 20rem),
        linear-gradient(135deg, #18130d, #090909);
    border: 1px solid rgba(200, 155, 60, 0.22);
    box-shadow: 0 24px 70px rgba(0, 0, 0, 0.38);
}

.card,
.browser-card,
.action-card,
.form-section,
.form-panel,
.filter-panel,
.table,
.detail-media,
.stock-side-card {
    background:
        linear-gradient(135deg, rgba(255,255,255,0.035), transparent),
        var(--color-card);
    border-color: rgba(200, 155, 60, 0.16);
    box-shadow: 0 16px 46px rgba(0,0,0,0.28);
}

.card:hover,
.browser-card:hover,
.action-card:hover,
a.card:hover,
.card.interactive:hover {
    border-color: rgba(200, 155, 60, 0.45);
    box-shadow: 0 24px 70px rgba(0,0,0,0.42);
}

.card-image-wrap {
    background:
        radial-gradient(circle at center, rgba(200, 155, 60, 0.12), transparent 60%),
        linear-gradient(135deg, #1a1713, #090909);
}

.card-image {
    transition: transform 320ms ease, filter 320ms ease;
}

a.card:hover .card-image {
    transform: scale(1.045);
    filter: saturate(1.06) contrast(1.04);
}

.card-body h2,
.card-body h3,
.section-title h2,
.browser-card-header h2 {
    color: #fff8ec;
}

.price {
    color: var(--color-accent);
}

.meta,
.section-title p,
.card-body p,
.browser-meta,
.field-help {
    color: var(--color-muted);
}

.badge {
    background: rgba(200, 155, 60, 0.14);
    color: #f3dfb0;
    border: 1px solid rgba(200, 155, 60, 0.18);
}

.button,
button.button {
    background: linear-gradient(135deg, #e2bd68, #b98524);
    color: #11100e;
    box-shadow: 0 14px 34px rgba(200, 155, 60, 0.20);
}

.button.secondary,
.dark-secondary {
    background: rgba(255,255,255,0.05);
    color: #fff8ec;
    border: 1px solid rgba(200, 155, 60, 0.22);
    box-shadow: none;
}

.button.secondary:hover,
.dark-secondary:hover {
    background: rgba(200, 155, 60, 0.12);
}

.table th {
    background: #100e0c;
    color: #c8b995;
}

.table td,
.table th {
    border-color: rgba(200, 155, 60, 0.12);
}

.form-row input,
.form-row textarea,
.form-row select {
    background: #100f0e;
    color: var(--color-text);
    border-color: rgba(200, 155, 60, 0.18);
}

.form-row input:focus,
.form-row textarea:focus,
.form-row select:focus {
    outline: none;
    border-color: rgba(200, 155, 60, 0.56);
    box-shadow: 0 0 0 4px rgba(200, 155, 60, 0.10);
}

.empty-state {
    background: rgba(255,255,255,0.035);
    border-color: rgba(200, 155, 60, 0.20);
}

@media (max-width: 920px) {
    .public-hero {
        grid-template-columns: 1fr;
        min-height: auto;
    }

    .public-hero-media img {
        min-height: 320px;
    }

    .public-usps {
        grid-template-columns: 1fr;
    }
}

/* Item thumbnails in beheer */

.browser-card-header.with-thumb {
    display: grid;
    grid-template-columns: 74px minmax(0, 1fr) auto;
    align-items: center;
    gap: 14px;
}

.item-thumb {
    width: 74px;
    height: 58px;
    border-radius: 14px;
    overflow: hidden;
    background:
        radial-gradient(circle at center, rgba(200, 155, 60, 0.14), transparent 60%),
        #0f0f0f;
    border: 1px solid rgba(200, 155, 60, 0.20);
    box-shadow: 0 10px 24px rgba(0,0,0,0.22);
    flex: 0 0 auto;
}

.item-thumb img,
.item-thumb video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.item-thumb-empty {
    width: 100%;
    height: 100%;
    display: grid;
    place-items: center;
    color: var(--color-accent);
    font-weight: 900;
    font-size: 22px;
    background: linear-gradient(135deg, #17120d, #090909);
}

.browser-title {
    min-width: 0;
}

.browser-title h2 {
    margin: 0;
}

.browser-title .meta {
    margin: 0 0 3px;
}

.selectable-media-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 12px;
}

.media-choice-card {
    display: grid;
    gap: 8px;
}

.media-choice-card img,
.media-choice-card video {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: 14px;
    border: 1px solid rgba(200, 155, 60, 0.18);
    background: #0f0f0f;
}

.media-choice-card span {
    display: inline-flex;
    justify-content: center;
    border-radius: 999px;
    padding: 6px 9px;
    background: rgba(200, 155, 60, 0.12);
    color: #f3dfb0;
    border: 1px solid rgba(200, 155, 60, 0.18);
    font-size: 12px;
    font-weight: 800;
}

@media (max-width: 720px) {
    .browser-card-header.with-thumb {
        grid-template-columns: 62px minmax(0, 1fr);
    }

    .browser-card-header.with-thumb > .badge {
        grid-column: 1 / -1;
        justify-self: start;
    }

    .item-thumb {
        width: 62px;
        height: 52px;
    }
}


/* UI polish: kaarten, labels, prijzen en filters */

.grid {
    align-items: stretch;
}

.card {
    display: flex;
    flex-direction: column;
}

.card-image-wrap {
    flex: 0 0 auto;
}

.card-body {
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 255px;
}

.card-body h2 {
    min-height: 2.45em;
    display: flex;
    align-items: flex-start;
}

.card-body > p:not(.meta):not(.price) {
    min-height: 4.9em;
}

.price {
    margin-top: auto;
    color: #e2bd68;
    font-size: 20px;
    letter-spacing: 0.01em;
}

.badges {
    min-height: 33px;
}

.badge {
    color: #fff3cf;
    background: rgba(200, 155, 60, 0.20);
    border-color: rgba(226, 189, 104, 0.34);
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.06);
}

.quick-actions {
    align-items: stretch;
}

.action-card {
    display: flex;
    flex-direction: column;
    min-height: 168px;
}

.action-card span {
    width: fit-content;
    background: linear-gradient(135deg, #e2bd68, #b98524);
    color: #090909;
    border: 1px solid rgba(255, 240, 190, 0.28);
    box-shadow: 0 8px 22px rgba(200, 155, 60, 0.16);
}

.action-card strong {
    color: #fff8ec;
    margin-top: 4px;
}

.action-card small {
    color: #cfc6b7;
    margin-top: auto;
}

.browse-tabs a {
    color: #fff8ec;
    background: rgba(255,255,255,0.055);
    border-color: rgba(200, 155, 60, 0.20);
}

.browse-tabs a:hover,
.browse-tabs a.active {
    color: #fff8ec;
    background: rgba(200, 155, 60, 0.18);
    border-color: rgba(226, 189, 104, 0.48);
}

.browse-tabs span {
    min-width: 30px;
    min-height: 30px;
    padding: 0 8px;
    background: linear-gradient(135deg, #e2bd68, #b98524);
    color: #090909;
    font-weight: 950;
    border: 1px solid rgba(255, 240, 190, 0.28);
}

.filter-panel label,
.form-row label {
    color: #fff8ec;
}

.filter-panel .form-row input,
.filter-panel .form-row select {
    color: #fff8ec;
    background: #0f0d0b;
    border-color: rgba(200, 155, 60, 0.28);
}

.filter-panel .form-row input::placeholder {
    color: #8f8678;
}

.pagination {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 14px;
    margin: 34px 0 10px;
}

.pagination-current {
    color: #f3dfb0;
    background: rgba(200, 155, 60, 0.12);
    border: 1px solid rgba(200, 155, 60, 0.22);
    border-radius: 999px;
    padding: 11px 16px;
    font-weight: 900;
}

/* Beheerkaarten iets rustiger en beter leesbaar */

.browser-card {
    min-height: 260px;
}

.browser-card-header.with-thumb {
    grid-template-columns: 74px minmax(0, 1fr) auto;
}

.browser-card-header.with-thumb .badge {
    align-self: start;
    white-space: nowrap;
}

.browser-title h2 {
    min-height: unset;
    color: #fff8ec;
}

.browser-title .meta {
    color: #c8b995;
}

.item-thumb {
    background: #090909;
}

.item-thumb img,
.item-thumb video {
    object-fit: cover;
}

/* Op kleinere schermen geen rare brede knoppen of kapotte cards */

@media (max-width: 760px) {
    .card-body {
        min-height: auto;
    }

    .card-body h2,
    .card-body > p:not(.meta):not(.price) {
        min-height: auto;
    }

    .price {
        margin-top: 12px;
    }

    .pagination {
        flex-wrap: wrap;
    }
}
